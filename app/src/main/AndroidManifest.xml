<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.programacion3.gruposinfinesdelucro.app">
    
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.FirstActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activities.LogInActivity"/>
        <activity android:name=".activities.SignUpActivity"/>
        <activity android:name=".activities.PerfilActivity"/>
        <activity android:name=".activities.NavigationActivity">
        </activity>
        <activity android:name=".activities.ActividadDiariaActivity"/>
        <activity android:name=".activities.RoutineExerciseActivity"/>
        <activity android:name=".activities.SetDurationActivity"/>
        <activity android:name=".activities.EjercicioActivity"/>
        <activity android:name=".activities.RoutinesActivity"/>
        <activity android:name=".activities.DaysActivity"/>
        <activity android:name=".activities.ShowAllExercises"/>
        <activity android:name=".activities.CreateRoutine"/>
        <activity android:name=".activities.ExplainActivity">
        </activity>
    </application>

</manifest>